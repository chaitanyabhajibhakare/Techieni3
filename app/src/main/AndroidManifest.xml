<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.techieni3.com.myapplication">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/CommonTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Main2Activity"
            android:label="View Pager" />
        <activity
            android:name=".ActivityOnIt"
            android:label="@string/title_activity_on_it"
            android:theme="@style/CommonTheme.NoActionBar" />
        <activity android:name=".PdfListActivity" />
        <activity android:name=".PdfActivity"
            android:theme="@style/CommonTheme.NoActionBar"></activity>
    </application>

</manifest>